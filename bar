#!/bin/bash

MONWIDTH=$(wattr w `lsw -r`)

newmails() {
	(ls "$MAIL/new"; ls ~/var/sit-mail/Inbox/new) | wc -l
}

# left bar
while :; do
	printf "%s%4s %3d %2d\n" "%{c}" "$(vol)" "$(bat)" "$(newmails)"
	sleep 1s
done |\

mybar 10 "bar" &

# right bar
while :; do
	printf "%s%(%d.%m  %I:%M)T\n" "%{c}"
	sleep 60s 
done |\

mybar $(( MONWIDTH - MONWIDTH / 15 - 10)) "bar" &

#!/bin/sh

on() {
	cat << EOF | bluetoothctl
power on
agent on
default-agent
EOF

}

off() {
	echo -e "power off\n" | bluetoothctl
}

case $1 in
	0)		off ;;
	1)		on ;;
	*)		echo "Usage: $(basename $0) <0|1>" ;;
esac

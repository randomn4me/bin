#!/bin/bash

res=$(echo -e "logout\nstandby\nreboot\nshutdown" | menu)

case "$res" in
	"logout")		ownlogout ;;
	"standby")	systemctl hibernate ;;
	"reboot")		systemctl reboot ;;
	"shutdown")	systemctl poweroff ;;
esac

exit 0
